Offline Voice Input delivers professional-grade speech-to-text that runs entirely on your device, with optional AI post-processing to clean transcripts afterwards. Pick from two on-device model sizes or skip the model entirely. Everything — audio capture, transcription, post-processing decisions — happens locally.

WHY THIS FORK

This release (v0.9.0) consolidates a year of divergence from the upstream `notune/android_transcribe_app` line. Three things characterise this build:

TRANSCRIPTION ENGINE
We kept the Rust ONNX pipeline (transcribe-rs) + NVIDIA Parakeet TDT 0.6B + Canary 180M Flash (INT8 quantized). Upstream moved to transcribe.cpp + Whisper; this fork does not.

• Fast (0.6B, ~640 MB) — best accuracy for general dictation
• Fastest (180M Flash, ~395 MB) — smaller footprint, fits older / low-RAM devices
• Use without model — register as the speech-to-text provider without using it; download later

Once downloaded, models live in the app sandbox and load via mmap with zero copy. NNAPI + XNNPACK providers on Android.

AI POST-PROCESSING (OPTIONAL)
Optional refinement pass that sends the raw transcript to any OpenAI-compatible endpoint (OpenAI, Gemini, Groq, LM Studio, local Ollama) with the prompt of your choosing.

• Multi-prompt templates — switch the active prompt per dictation (formal tone, casual tone, list-cleanup, target-language) without editing inline
• Edit, duplicate, import, export prompts as JSON
• Default app prompt is editable + exportable — customise once; "Reset to default" returns to the canonical version
• Default "My words" dictionary is editable + exportable — same model
• Built-in prompt is tuned for speech-to-text repair (self-corrections, filler removal, ITN, list-formatting) without paraphrasing

MODEL SELECTION UX
First-run welcome dialog shows the three options with their size inline (e.g. "Fast / 0.6B"), so the trade-off is visible immediately. Switch models anytime from the home screen. Skip the model on first launch if you only want the speech-to-text provider registered.

VOICE TYPING IN ANY APP
Tap the microphone on a compatible keyboard (Microsoft SwiftKey, HeliBoard, OpenBoard, or any keyboard that lets you pick the speech provider) — Offline Voice Input transcribes straight into the text field. A dedicated voice keyboard is included for when you'd rather use it on its own.

LIVE SUBTITLES / TRANSCRIBE AUDIO FILES
On-device captions for any audio/video playing on your phone. Share an audio file (or open it with the app) to turn the recording into text.

PRIVACY
• Audio never leaves your device during transcription
• Post-processing is off by default — only the raw transcript text is sent to the URL you configure, over HTTPS to whatever endpoint you choose
• No analytics, no crash reporting, no remote telemetry, no account

LANGUAGES
Bulgarian, Croatian, Czech, Danish, Dutch, English, Estonian, Finnish, French, German, Greek, Hungarian, Italian, Latvian, Lithuanian, Maltese, Polish, Portuguese, Romanian, Slovak, Slovenian, Spanish, Swedish, Russian, Ukrainian.

BUILT ON OPEN SOURCE
• NVIDIA Parakeet TDT 0.6B and Canary 180M Flash (INT8 ONNX quantized by istupakov)
• transcribe-rs — Rust transcription engine by CJ Pais
• ONNX Runtime 1.25.0
• A native Android interface with a Rust core

LICENSE: MIT.

Source code, issues, and discussions:
https://github.com/marodriguezd/android_transcribe_app

Note: this fork shares the package name `dev.notune.transcribe` with the upstream build, so the two cannot coexist on the same device. Pick one.
