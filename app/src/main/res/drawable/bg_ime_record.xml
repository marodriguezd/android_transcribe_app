<?xml version="1.0" encoding="utf-8"?>
<!-- Circular record button surface. Solid white so it can be recolored at runtime
     via backgroundTint (primaryContainer idle, errorContainer while recording). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@android:color/white" />
</shape>
